import io.pivotal.pal.tracker.oauthserver.gradlebuild.CfMigrationPlugin
import io.pivotal.pal.tracker.oauthserver.gradlebuild.LocalMigrationPlugin

subprojects {
    apply plugin: LocalMigrationPlugin
    apply plugin: CfMigrationPlugin
}
